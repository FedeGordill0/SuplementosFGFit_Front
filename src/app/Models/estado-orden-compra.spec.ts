import { EstadoOrdenCompra } from './estado-orden-compra';

describe('EstadoOrdenCompra', () => {
  it('should create an instance', () => {
    expect(new EstadoOrdenCompra()).toBeTruthy();
  });
});
