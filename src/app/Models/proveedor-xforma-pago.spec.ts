import { ProveedorXFormaPago } from './proveedor-xforma-pago';

describe('ProveedorXFormaPago', () => {
  it('should create an instance', () => {
    expect(new ProveedorXFormaPago()).toBeTruthy();
  });
});
